schema_version: microi2i.inference_config.v1
workflow: kikuchi_inference
output_root: artifacts/runs
dry_run: false
inference:
  legacy_script: run_kikuchi_inference.py
  legacy_args: []
notes:
  purpose: Kikuchi inference migration wrapper. Parameterized config will replace script defaults in a later phase.
