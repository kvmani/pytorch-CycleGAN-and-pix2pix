schema_version: microi2i.inference_config.v1
workflow: ebsd_inference
output_root: artifacts/runs
dry_run: false
inference:
  legacy_script: run_ebsd_inference.py
  expected_output_dir: artifacts/inference/ebsd/test_latest/images
  legacy_args:
    - --input_folder
    - ./datasets/ebsd_inference
    - --results_dir
    - artifacts/inference/ebsd
    - --model_name
    - ebsd_model
    - --target_size
    - "256"
notes:
  purpose: EBSD inference migration wrapper with explicit parameters.
